# Web (Next.js) Environment Variables (copy to .env.local)

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_ORCHESTRATOR_URL=
NEXT_PUBLIC_SUPERADMIN_EMAIL=

# Server-side only (used by middleware/routes if needed)
# SUPABASE_SERVICE_ROLE_KEY is NOT required for /api/enqueue fallback anymore,
# but keep for potential admin jobs or maintenance scripts.
SUPABASE_SERVICE_ROLE_KEY=
